---
layout: base
pageTitle: fe-assemble
isIndex: true
---

{{package.name}}
{{package.description}}
{{package.version}}

{{#each navigation}}
  {{title}}
  {{#each items}}
    {{subTitle}} ({{subItems.length}})
    {{#each subItems}}
      {{title}} - {{href}}
    {{/each}}
  {{/each}}
{{/each}}
